import React from 'react'
import { StyledButton } from './styles/StyledButton'

function Button() {
    return (
        <StyledButton>
            
        </StyledButton>
    )
}

export default Button
